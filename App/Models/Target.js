export default class State {
  constructor(data) {
    this.honey = data.honey
    this.clickmod = data.clickmod
    this.automod = data.automod
    this.tools = data.tools
    this.upgrade = data.upgrade
  }
}